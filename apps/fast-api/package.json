{
  "name": "fast-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "install-python-deps": "pip install -r requirements.txt",
    "build": "pnpm install-python-deps"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}